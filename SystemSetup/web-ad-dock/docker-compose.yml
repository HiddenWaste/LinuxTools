services:
  app:
    build: .
    container_name: ubuntu-php-env
    # Maps the current directory to /app inside the container
    volumes:
      - .:/app
    # Keeps the container running in the background
    tty: true
    stdin_open: true
